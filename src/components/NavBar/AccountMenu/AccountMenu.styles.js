import { MenuItem, styled } from "@mui/material";

export const StyledMenuItem = styled(MenuItem)({
    fontWeight: "bold",
    fontSize: "13px",
  "&:hover": {
    backgroundColor: "#f5dcffaa",
  },
});
